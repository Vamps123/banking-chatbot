# Frontend Chat UI

The React frontend connects to the backend API and renders the banking support chat interface.

## Local Run

```bash
cd frontend
npm install
npm run dev -- --host 0.0.0.0
```

## Build

```bash
npm run build
```

## Configuration

Set `VITE_API_URL` in `.env` if your backend runs on a different host or port.
